function sayHello() {
    var compiler = document.getElementById('compiler').nodeValue;
    var framework = document.getElementById('framework').nodeValue;
    return "Hello from " + compiler + " and " + framework;
}
